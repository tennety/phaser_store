%h1 Phaser Catalog
%table#catalog
  %tr
    %th Phaser Type
    %th Price
    %th In Stock
    %th Purchase
  - phasers.each do |phaser|
    %tr
      %td
        = phaser.name
      %td
        = phaser.price
      %td
        = phaser.quantity
      %td
        %form.checkout{:action => 'https://www.2checkout.com/checkout/spurchase', :method => 'post'}
          %input{:type => 'number', :value => 1, :name => 'order[qty]'}
          %input{:type => 'submit', :value => 'Checkout with 2CO'}
